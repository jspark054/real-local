# PRD — REAL LOCAL

> Product Requirements Document · v1.1
> 기반 문서: `기획서_로컬리얼.md` (2026-07-25, 클라이언트 박진선 미팅 + 추가 메모)
> 작성일: 2026-07-25 (최종 수정: 2026-08-03)

---

## 0. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서 목적 | 기획서(요구사항 요약)를 실행 가능한 개발 스펙으로 전환 |
| 대상 독자 | 개발자(프론트/백엔드), 디자이너, 클라이언트 검수용 |
| 상태 | Draft v1.1 — §11 "확정 필요 사항"은 클라이언트 컨펌 전까지 가정치로 진행 |
| 관련 문서 | `기획서_로컬리얼.md` |

### 변경 이력

| 날짜 | 버전 | 변경 내용 | 변경 사유 |
|---|---|---|---|
| 2026-07-25 | v1.0 | 최초 작성 | 기획서 기반 초안 |
| 2026-08-03 | v1.1 | 맵 승인 워크플로우 추가(FR-7.1, §5.3, §5.4), 홈 화면 지역 필터(칩) 보조 탐색 수단 반영(§1, §8.1) | 클라이언트 피드백(과제 추가 고려사항) 반영 |

---

## 1. 배경 및 문제 정의

방한 외국인 관광객은 한국 로컬 맛집 정보를 주로 구글맵 등 일반 검색에 의존하는데, 이는 "그 지역 사람이 실제로 인정한 곳"이라는 신뢰 신호가 없다. REAL LOCAL은 **승인된 로컬 크리에이터가 주제를 정해 큐레이션한 맵**을 통해 이 신뢰 공백을 채운다.

**한 줄 정의**: 승인된 로컬 크리에이터가 만든 "찐맛집 맵"을 방한 외국인에게 보여주는 서비스.

**차별점**: 타겟이 매우 좁고 명확함(방한 외국인) + "지역 기반 큐레이션"이 아니라 "**크리에이터 개인의 취향/주제 기반 큐레이션**"이라는 구조.

> **참고 (v1.1)**: 핵심 컨셉은 크리에이터 취향/주제 기반 큐레이션을 유지하되, 홈 화면에는 사용자가 원하는 지역의 맵을 빠르게 찾을 수 있도록 **지역 기반 필터(칩 형태)**를 보조 탐색 수단으로 함께 제공한다. 맵의 1차 정체성은 여전히 "주제"이며, 지역 필터는 그 위에 얹는 편의 기능이다.

---

## 2. MVP 완료 기준 (Definition of Done)

이 프로젝트는 클라이언트 과제 형태로, 화요일/목요일 두 번의 체크포인트가 있다. 정식 비즈니스 KPI 대신 아래를 성공 기준으로 삼는다.

- **화요일 오전 체크포인트**: §8 화면 스펙에 정의된 3개 핵심 화면(메인/맵 상세/저장 탭)이 와이어프레임 또는 동작하는 프론트엔드로 시연 가능해야 함
- **목요일 체크포인트**: §6 기능 요구사항의 Must 항목이 백엔드(클라우드 DB) 연동 상태로 동작해야 함
- **공통 조건**: 클라이언트가 미팅 중 직접 언급한 4개 화면 흐름(맵 목록 → 맵 상세 → 저장 → 로그인)이 끊김 없이 동작

---

## 3. 스코프

### 3.1 In Scope (MVP)
맵 목록 열람 · 맵 상세(지도+장소 리스트) · 맵 저장 · 맵/장소 공유 · 맵 단위 후기 · 로그인(일반회원/크리에이터) · 크리에이터 맵·장소 등록 · 어드민 크리에이터 승인 · 클라우드 DB 연동

### 3.2 Out of Scope (명시적 제외)
실시간 위치추적 · 크리에이터 소개 페이지 · 장소별 순위 · 결제 · 네이티브 앱 · 크리에이터 셀프 가입(직접 회원가입으로 크리에이터가 되는 플로우) · 장소별 개별 후기 · 소셜 로그인(§11에서 재확인 필요)

---

## 4. 유저 및 역할

| 역할 | 정의 | 진입 방법 |
|---|---|---|
| 비로그인 방문자 | 서비스를 둘러보기만 하는 사용자 | 별도 가입 없이 웹 접속 |
| 일반회원 | 로그인하여 저장/공유/후기 작성하는 사용자 | 이메일 회원가입 |
| 크리에이터 | 맵을 생성하고 장소를 등록하는 승인된 사용자 | **어드민이 수작업으로 계정 생성/역할 부여** (셀프 가입 없음) |
| 어드민 | 크리에이터 승인·초대를 관리하는 운영자(클라이언트) | 별도 어드민 계정(초기 시딩) |

역할은 `User.role` 단일 필드로 관리하며(`general` / `creator` / `admin`), 일반회원 → 크리에이터 전환은 UI가 아니라 **DB 값 직접 변경 또는 어드민 화면을 통한 승인**으로 이루어진다.

---

## 5. 핵심 유저 플로우

### 5.1 비로그인 탐색
1. 메인 접속 → 맵 목록 확인
2. 맵 카드 클릭 → 맵 상세(지도+장소 리스트) 열람
3. 저장/후기 시도 시 → 로그인 화면으로 유도

### 5.2 일반회원 — 저장 및 후기
1. 로그인
2. 맵 상세에서 "맵 저장" 클릭 → 저장 탭에 반영
3. 장소 카드에서 "장소 저장" 클릭 → 저장 탭의 장소 목록에 반영 (맵 저장과 별개 리스트)
4. 맵 또는 장소의 "공유" 클릭 → 공유 링크 생성/복사
5. 맵 상세 하단에서 "이 맵에 대한 후기 작성" → 별점 + 코멘트 등록 (장소별 후기는 없음)

### 5.3 크리에이터 — 맵/장소 등록
1. 어드민이 사전에 크리에이터 계정 생성 및 역할 부여 (**크리에이터 자격 자체에는 별도 승인 절차 없음** — 계정 생성 시점에 이미 역할이 부여됨)
2. 크리에이터 로그인 → "내 맵" 화면에서 맵 생성 (제목 + 한 줄 설명)
3. 맵 안에 장소 추가 (이름, 주소/좌표, 설명)
4. 맵을 "제출(submit)"하면 상태가 `pending`(승인 대기)으로 전환됨
5. 어드민이 승인하기 전까지 해당 맵은 홈 화면 등 일반 사용자 노출 영역에 나타나지 않음
6. 어드민 승인(`published`) 시점에 비로소 일반 사용자에게 노출됨

> **v1.1 변경**: (구) "저장 시 즉시 일반 사용자에게 노출, 별도 승인 절차 없음 — MVP" → (신) "맵 단위 어드민 승인 필요". 클라이언트 피드백(과제 추가 고려사항)에 따라, 크리에이터 자격 승인과는 별개로 **맵 개별 건에 대한 어드민 검수 절차**를 MVP 범위에 포함하도록 변경.

### 5.4 어드민 — 크리에이터 승인/초대 및 맵 검수
1. 어드민 로그인 → "크리에이터 관리" 화면
2. 신규 크리에이터 계정 생성(이메일 지정) 또는 기존 일반회원의 역할을 `creator`로 변경
3. 크리에이터 목록에서 활성/비활성 상태 관리
4. "검수 대기(To review)" 목록에서 크리에이터가 제출한 맵을 확인 → 승인(published) 또는 반려(rejected)
5. 승인된 맵만 홈 화면 등 일반 사용자 노출 영역에 노출됨

---

## 6. 기능 요구사항

우선순위: **M**=Must(목요일까지 필수) · **S**=Should(여유 있으면) · **C**=Could(다음 라운드)

### 6.1 맵 목록 / 탐색
| ID | 요구사항 | 인수 조건 | 우선순위 |
|---|---|---|---|
| FR-1.1 | 메인에서 전체 맵을 카드 리스트로 노출 | 맵의 제목, 한 줄 설명, 대표 이미지(있으면) 노출 | M |
| FR-1.2 | 비로그인 사용자도 목록 열람 가능 | 인증 없이 GET 가능 | M |
| FR-1.3 | 맵 카드 클릭 시 맵 상세로 이동 | 라우팅 정상 동작 | M |

### 6.2 맵 상세
| ID | 요구사항 | 인수 조건 | 우선순위 |
|---|---|---|---|
| FR-2.1 | 상단에 Google Maps 지도 표시, 맵에 속한 모든 장소를 핀으로 표시 | 장소 좌표 기준 핀 렌더링, 지도 초기 중심/줌은 핀 전체가 보이도록 fit-bounds | M |
| FR-2.2 | 하단에 장소 상세 리스트(이름/설명/주소) 표시 | 지도의 핀과 리스트 항목이 1:1 매핑 | M |
| FR-2.3 | 핀 또는 리스트 항목 클릭 시 서로 하이라이트 연동 | 클릭 시 지도 포커스 이동 또는 리스트 스크롤 | S |
| FR-2.4 | 맵 한 줄 설명을 상단에 노출 | 맵 생성 시 입력한 description 그대로 표시 | M |
| FR-2.5 | 장소 정렬은 등록 순서(순위 기능 없음) | 별도 정렬 UI 없음 | M |

### 6.3 인증
| ID | 요구사항 | 인수 조건 | 우선순위 |
|---|---|---|---|
| FR-3.1 | 이메일/비밀번호 기반 회원가입·로그인 | 일반회원 대상. 이메일 형식 검증, 비밀번호 8자 이상 | M |
| FR-3.2 | 로그인 세션 유지 | 새로고침/재방문 시 로그인 상태 유지(토큰/세션) | M |
| FR-3.3 | 크리에이터/어드민 계정은 셀프 가입 UI 없이 시딩(seed) 또는 어드민 화면으로만 생성 | 회원가입 화면에서 역할 선택 불가 | M |
| FR-3.4 | 비밀번호 재설정 | (§11 확정 필요 — MVP 제외 시 "재설정 불가, 재가입 안내"로 대체) | C |
| FR-3.5 | 소셜 로그인 | (§11 확정 필요) | C |

### 6.4 저장
| ID | 요구사항 | 인수 조건 | 우선순위 |
|---|---|---|---|
| FR-4.1 | 로그인한 일반회원이 맵을 저장/저장 취소할 수 있음 | 토글 동작, 중복 저장 방지 | M |
| FR-4.2 | 로그인한 일반회원이 장소를 저장/저장 취소할 수 있음 | 맵 저장과 별개 목록으로 관리 | M |
| FR-4.3 | 저장 탭에서 "저장한 맵" / "저장한 장소" 두 섹션으로 구분 표시 | 탭 또는 세그먼트로 구분 | M |

### 6.5 공유
| ID | 요구사항 | 인수 조건 | 우선순위 |
|---|---|---|---|
| FR-5.1 | 맵 상세에서 해당 맵의 공유 링크 생성/복사 | 클릭 시 URL 클립보드 복사 + 토스트 안내 | M |
| FR-5.2 | 장소 카드에서 해당 장소의 공유 링크 생성/복사 | 딥링크로 접속 시 해당 맵 상세 화면이 열리고 해당 장소가 하이라이트 | S |
| FR-5.3 | 저장 리스트 자체의 공유 기능 | 불필요 (Out of Scope) | — |

### 6.6 후기 (맵 단위)
| ID | 요구사항 | 인수 조건 | 우선순위 |
|---|---|---|---|
| FR-6.1 | 로그인한 일반회원이 맵 전체에 대해 별점(1~5)과 코멘트를 남길 수 있음 | 맵당 사용자 1개 후기(수정 가능, 중복 작성 불가) | M |
| FR-6.2 | 맵 상세에 평균 별점과 후기 목록 노출 | 평균 별점 계산은 단순 산술 평균 | M |
| FR-6.3 | 장소별 개별 후기 | 불필요 (Out of Scope) | — |

### 6.7 크리에이터 — 맵/장소 관리
| ID | 요구사항 | 인수 조건 | 우선순위 |
|---|---|---|---|
| FR-7.1 | 크리에이터가 맵을 생성(제목 + 한 줄 설명)하고 검수를 위해 제출 | 저장 시 `draft` 상태로 목록(내 맵)에 반영, "제출" 액션 시 `pending`으로 전환. 어드민 승인(`published`) 전까지 홈 등 일반 노출 영역에는 표시되지 않음 | M |
| FR-7.2 | 크리에이터가 자신의 맵에 장소를 추가(이름/주소·좌표/설명) | 주소 입력 시 좌표 확보 방법 필요(§11) | M |
| FR-7.3 | 크리에이터가 자신이 만든 맵 목록을 조회/수정/삭제 | 본인 소유 맵만 접근 가능 | M |
| FR-7.4 | 크리에이터 소개 페이지 | 불필요 (Out of Scope) | — |
| FR-7.5 | 장소/맵 순위 지정 | 불필요 (Out of Scope) | — |

### 6.8 어드민
| ID | 요구사항 | 인수 조건 | 우선순위 |
|---|---|---|---|
| FR-8.1 | 어드민이 신규 크리에이터 계정을 생성하거나 기존 유저 role을 `creator`로 변경 | 어드민 전용 화면/기능, 일반 유저는 접근 불가 | M |
| FR-8.2 | 어드민이 크리에이터 목록 및 각 크리에이터가 만든 맵 수를 확인 | 리스트 뷰 | S |
| FR-8.3 | 어드민이 맵/장소를 직접 편집(비상 대응용) | DB 직접 수정으로 대체 가능(MVP) | C |
| FR-8.4 | 어드민이 크리에이터가 제출(`pending`)한 맵을 검수하여 승인(`published`)/반려(`rejected`) | 승인 전 맵은 일반 사용자에게 노출되지 않음, 승인/반려 이력 확인 가능 | M |

---

## 7. 데이터 모델

```
User
 - id (PK)
 - email (unique)
 - password_hash
 - display_name
 - role: enum('general','creator','admin')
 - created_at

Map
 - id (PK)
 - creator_id (FK -> User.id)
 - title
 - description        // 맵 한 줄 설명
 - created_at
 - updated_at

Spot
 - id (PK)
 - map_id (FK -> Map.id)
 - name
 - address
 - lat, lng
 - description
 - created_at

SavedMap
 - id (PK)
 - user_id (FK -> User.id)
 - map_id (FK -> Map.id)
 - created_at
 - unique(user_id, map_id)

SavedSpot
 - id (PK)
 - user_id (FK -> User.id)
 - spot_id (FK -> Spot.id)
 - created_at
 - unique(user_id, spot_id)

MapReview
 - id (PK)
 - map_id (FK -> Map.id)
 - user_id (FK -> User.id)
 - rating (1~5)
 - comment (text, optional)
 - created_at
 - updated_at
 - unique(map_id, user_id)   // 맵당 1인 1후기
```

**관계 요약**: `User(creator) 1—N Map`, `Map 1—N Spot`, `User(general) N—N Map`(via SavedMap), `User(general) N—N Spot`(via SavedSpot), `User(general) 1—N MapReview` per map.

---

## 8. 화면 스펙

### 8.1 메인 (맵 목록)
- 상단: 서비스 로고/타이틀
- 지역 필터: 맵 카드 리스트 위에 지역 필터 칩(예: 지역명 단위) 노출 — 주제 기반 큐레이션을 대체하는 것이 아니라 보조하는 탐색 수단(§1 참고)
- 본문: 맵 카드 리스트 (세로 스크롤) — 각 카드: 대표 이미지(optional) / 제목 / 한 줄 설명 / 평균 별점 (선택된 지역 필터가 있으면 해당 지역 맵만 노출)
- 하단: 탭바 — 홈 / 저장 / 마이(로그인 상태에 따라 로그인 버튼으로 대체)
- 빈 상태: 맵이 0개일 경우(또는 필터 결과 0개일 경우) "아직 등록된 맵이 없어요" / "해당 지역에 맵이 없어요" 안내

### 8.2 맵 상세
- 상단: Google Maps (핀 = 장소, fit-bounds로 전체 핀 노출)
- 지도 아래: 맵 제목, 한 줄 설명, 평균 별점, 저장/공유 버튼
- 하단: 장소 리스트 (이름/설명/주소, 각 항목에 저장/공유 아이콘)
- 최하단: 후기 섹션 — 별점 입력 + 코멘트 작성 폼, 기존 후기 목록
- 로그인 필요한 액션(저장/공유/후기)을 비로그인 상태에서 시도하면 로그인 화면으로 유도

### 8.3 저장 탭
- 세그먼트 컨트롤: "저장한 맵" / "저장한 장소"
- 각 섹션은 카드 리스트, 클릭 시 해당 상세로 이동
- 비로그인 상태 접근 시 로그인 유도 화면

### 8.4 로그인 / 회원가입
- 이메일 + 비밀번호 입력, 기본 유효성 검사
- 회원가입 시 역할은 자동으로 `general` (선택 UI 없음)

### 8.5 크리에이터 — 내 맵 관리 (크리에이터 로그인 시에만 노출)
- 내가 만든 맵 리스트 + "새 맵 만들기" 버튼
- 맵 생성/수정 폼: 제목, 한 줄 설명
- 맵 상세 진입 시 장소 추가/수정/삭제 폼: 이름, 주소(좌표 변환), 설명

### 8.6 어드민 — 크리에이터 관리 및 맵 검수 (어드민 로그인 시에만 노출)
- 검수 대기(To review): 크리에이터가 제출(`pending`)한 맵 목록, 승인/반려 액션
- 유저 검색 + role 변경(→ creator) 또는 신규 계정 생성 폼
- 크리에이터 목록 + 보유 맵 수

---

## 9. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| 플랫폼 | 모바일 웹 우선(반응형), 데스크탑 대응은 필수 아님 |
| 배포 방식 | 앱스토어 아님, 웹 URL 접속. 개발팀 자체 배포, 도메인은 클라이언트가 추후 구매·연결 |
| 언어 | UI 텍스트는 영어 기본, 한국어 병기 여부는 §11에서 확정 |
| 데이터 저장 | 클라우드 DB 필수(기기 로컬 저장 금지) — 로그인 기반으로 기기 무관 접근 |
| 지도 | Google Maps JavaScript API (또는 Embed API) 연동 |
| 인증 보안 | 비밀번호 해시 저장(bcrypt 등), 평문 저장 금지 |
| 브라우저 | 최신 Chrome/Safari 모바일 브라우저 기준 |
| 성능 | 맵 목록/상세 최초 로딩 3초 이내(일반 4G 기준, 목표치) |

---

## 10. 기술 아키텍처 개요

- **프론트엔드**: 반응형 웹(SPA 또는 SSR 프레임워크), 모바일 뷰 우선 레이아웃
- **백엔드**: REST(또는 서버리스 함수) API, 클라우드 DB(Postgres/Firestore 등 팀 재량)와 연동
- **지도**: Google Maps API 키 발급 필요 — 사용량 기반 과금이므로 예산(400만원) 내 무료 티어 범위 확인 필요
- **인증**: 이메일/비밀번호 + 세션 토큰(JWT 등)
- **배포**: 프론트/백엔드 각각 관리형 호스팅(Vercel/Netlify + 서버리스 DB 등)으로 자체 배포, 클라이언트 서버 제공 없음
- **도메인**: 배포 초기엔 플랫폼 기본 서브도메인 사용, 클라이언트 구매 후 커스텀 도메인 연결

---

## 11. 확정 필요 사항 (Assumptions to confirm)

이 PRD는 아래 항목을 **임시 기본값으로 가정**하고 작성됨. 클라이언트 컨펌 전까지는 이 가정으로 진행.

| 항목 | 현재 가정 | 확정 필요 이유 |
|---|---|---|
| 회원가입 인증 방식 | 이메일+비밀번호만, 소셜 로그인 없음 | 미팅에서 전혀 논의되지 않음 |
| 비밀번호 재설정 | MVP 제외(재가입 안내로 대체) | 시간·예산 제약 고려한 임시 결정 |
| 이메일 인증(가입 확인 메일) | MVP 제외 | 동일 |
| 장소 좌표 입력 방식 | 주소 입력 → 지오코딩 API로 좌표 변환 | 크리에이터가 직접 위도/경도를 입력할 수 없다고 가정 |
| 후기 형식 | 별점(1~5) + 텍스트 코멘트 | 미팅에서 "후기"라고만 언급, 형식 미지정 |
| 브랜드 컬러 | 베이지~화이트 팔레트 초안 필요 | 클라이언트 레퍼런스 가이드북 미수령 |
| 초기 데이터 | 클라이언트 제공 "맵 모음 파일" 대기 중 | 실데이터 없이는 카테고리/필드 확정 불가 |
| 한국어 병기 여부 | UI는 영어 단일 언어로 진행 | 미확정 |
| 경쟁사 분석 | 미실시 | 클라이언트도 명확한 경쟁사 인지 없음 |

---

## 12. 마일스톤

| 시점 | 산출물 |
|---|---|
| 화요일 오전 | §8.1~8.3 화면의 와이어프레임 또는 동작 프론트엔드 데모 |
| 목요일 | §6 Must 항목 전체 + 클라우드 DB 연동 완료본 제출 |
| 제출 후 2주 | 유지보수(버그 수정 한정, 신규 기능 제외) |

---

## 13. 리스크

- **시간/예산 제약(400만원, ~4일)**: 인증·지오코딩·지도 API 연동을 동시에 처리해야 해 일정 리스크 높음 → §11 가정치로 회원가입 플로우를 최대한 단순화해 리스크 완화
- **실데이터 미수령**: 장소 카테고리별 필드가 실데이터 확인 전까지 미확정 → 스키마를 최소 공통 필드(이름/주소/설명)로 시작하고 확장 가능하게 설계 권장
- **Google Maps API 비용**: 무료 티어 초과 시 예산 압박 가능 → 사용량 모니터링 필요

---

## 14. 용어집

- **맵(Map)**: 크리에이터가 특정 주제로 묶은 장소들의 컬렉션 (지역 단위 아님)
- **장소(Spot)**: 맵에 속한 개별 맛집/술집 등 지점
- **크리에이터**: 맵을 생성할 수 있는 승인된 유저
- **어드민**: 크리에이터 승인/관리를 담당하는 운영 계정
